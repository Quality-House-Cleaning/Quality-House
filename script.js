const menuButton=document.querySelector('.menu-toggle'),nav=document.querySelector('.main-nav');menuButton?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));menuButton.textContent=open?'×':'☰'});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menuButton?.setAttribute('aria-expanded','false');if(menuButton)menuButton.textContent='☰'}));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('visible')}),{threshold:.08});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));const form=document.getElementById('estimate-form');form?.addEventListener('submit',event=>{event.preventDefault();const data=new FormData(form),subject='Free Estimate Request - '+(data.get('name')||'Quality House Cleaning'),body=[`Full Name: ${data.get('name')||''}`,`Phone: ${data.get('phone')||''}`,`Email: ${data.get('email')||''}`,`Preferred Contact Method: ${data.get('contactMethod')||''}`,`Bedrooms & Bathrooms: ${data.get('rooms')||''}`,`Approx. Square Footage: ${data.get('sqft')||''}`,`Service Type: ${data.get('service')||''}`,`Preferred Date & Time: ${data.get('datetime')||''}`,'','Specifications:',`${data.get('specs')||''}`].join('\n');window.location.href=`mailto:qualityhouse2027@gmail.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`});